# Agent-LLM-Stage 框架搭建报告

> 本报告记录依据 `Agent-LLM-Stage-开发框架与实现方案.md` 搭建项目框架的过程、实现细节与阶段一验证结果。
> 生成日期：2026-08-03

## 1. 概述

已按设计文档完成**整个项目框架搭建**，并将**开发计划阶段一（Graph 骨架与模拟执行）全部落地并通过验证**。

- **一个 Agent、一个 StateGraph、12 个节点**（含文档第 4 节的失败报告分支）。
- 用 **Fake Adapter** 模拟三个被测模型，验证了节点路由、失败分支、人工审批中断与 checkpoint 恢复。
- 完成 CLI：`run` / `status` / `resume` / `report`。
- 为路由函数、状态更新、指标函数、执行器幂等性、图集成编写了 **36 个单元/集成测试，全部通过**。
- 阶段二/三的真实 Adapter、Repository、API、Docker 等以脚手架形式预留接口。

核心能力链路已跑通：

```text
理解评测任务 -> 生成执行计划 -> 校验与审批 -> 并发调用模型
-> 规则评分 + Judge 盲评 -> 汇总指标 -> 解释结果 -> 生成报告
```

## 2. 环境与依赖

- Python **3.12.9**，独立虚拟环境 `.venv/`（项目内）。
- 因 pypi.org 连接异常，依赖通过**清华镜像**安装。
- 依赖在 `pyproject.toml` 中锁定，按阶段拆分为 extras：

| 分组 | 内容 | 状态 |
| --- | --- | --- |
| 核心依赖 | langgraph 1.2.10、langchain-core 1.5.3、pydantic 2.13.4、typer、pyyaml | 已安装 |
| `[dev]` | pytest、pytest-asyncio、ruff | 已安装 |
| 持久化 checkpoint | langgraph-checkpoint-sqlite 3.1.1（Agent 状态，非业务数据） | 已安装 |
| `[phase2]` | langchain-openai、httpx | 预留，未安装 |
| `[phase3]` | sqlalchemy、psycopg、supabase、fastapi、uvicorn、jinja2、plotly、langgraph-checkpoint-postgres | 预留，未安装 |

安装命令：`pip install -e ".[dev]"`（外加 `langgraph-checkpoint-sqlite`）。

## 3. 目录结构

```text
app/
  __init__.py
  cli.py                  # Typer CLI：run / status / resume / report
  utils.py                # load_dataset（路径白名单+Schema 校验）、new_run_id
  graph/
    state.py              # EvaluationState（TypedDict，全量替换语义）
    graph.py              # StateGraph 组装与条件边
    nodes.py              # 12 个节点函数
    routes.py             # 3 个路由函数
    checkpointer.py       # memory / sqlite / postgres 工厂
  domain/
    schemas.py            # Pydantic 数据契约（StrEnum + model_dump(mode=json)）
  adapters/
    base.py               # ModelAdapter 协议 + 工厂注册表
    fake.py               # FakeAdapter：alpha/beta/gamma + judge
    provider_a.py         # 阶段二脚手架（OpenAI 兼容）
    provider_b.py         # 阶段二脚手架（httpx）
  services/
    executor.py           # asyncio 并发、每模型 Semaphore、指数退避、request_key 去重
    scoring.py            # 规则评分 + Judge 双次盲评
    metrics.py            # P50/P95、成本、失败率、评分方差、异常标记（纯函数）
    reports.py            # Markdown 报告
  repositories/
    runs.py               # JSON 文件落盘，按 request_key 幂等 upsert
  api/
    main.py               # FastAPI 骨架（阶段三）
datasets/dataset_v1.jsonl # 5 条示例测试集
configs/run.example.yaml  # 运行配置示例
prompts/                  # planner_v1 / judge_v1 / analyst_v1 提示词
tests/                    # 6 个测试文件，36 个用例
docker-compose.yml        # PostgreSQL（阶段三）
pyproject.toml / README.md / .env.example / .gitignore
```

## 4. 核心实现说明

### 4.1 数据契约（`app/domain/schemas.py`）

按文档 8.3 节定义全部 Pydantic 模型：`Rubric`、`TestCase`、`ModelConfig`、`JudgeConfig`、`RunConfig`、`EvaluationPlan`、`TaskItem`、`ModelResult`、`JudgeResult`、`ScoreResult`、`ModelMetrics`、`Anomaly`、`Metrics`、`ErrorRecord`。

关键决策：
- 枚举使用 `StrEnum`（`TaskStatus`/`RiskFlag`/`RunStatus`），成员即字符串，序列化友好。
- 时间字段用 Unix epoch（float）而非 `datetime`，确保 checkpoint 可序列化。
- `EvaluationPlan` 含 `requires_clarification` / `missing_fields`，对齐文档"缺信息不得猜测填充"。

### 4.2 Agent State（`app/graph/state.py`）

`EvaluationState` 为 `TypedDict(total=False)`，节点可返回部分更新。**列表字段不使用 add reducer，采用"全量替换"语义**——节点返回完整列表，失败重跑不会重复累加，配合 executor 的 `request_key` 去重实现幂等。在文档 8.1 基础上增加 `config`、`analysis`、`approval_decision`、`current_node` 字段以支撑流程。

### 4.3 StateGraph 与节点（`app/graph/nodes.py`、`graph.py`）

12 个节点，职责单一：

| 节点 | 职责 | LLM 决策？ |
| --- | --- | --- |
| intake_request | 解析 YAML 配置，生成 run_id | 否 |
| plan_evaluation | 产出 EvaluationPlan（阶段一确定性） | 是（受 Schema 约束，阶段二接 LLM） |
| validate_plan | 数据集/白名单/预算校验，判定审批 | 否 |
| human_approval | `interrupt()` 暂停等待人工确认 | 否 |
| build_tasks | case × model 矩阵 + 稳定 request_key | 否 |
| execute_models | 并发执行、限流、重试、幂等去重 | 否 |
| validate_outputs | 失败归档，判定是否有可评分候选 | 否 |
| score_results | 规则评分 + Judge 双次盲评 | 是（Judge，阶段二接 LLM） |
| aggregate_metrics | 指标聚合、方差、异常标记（纯函数） | 否 |
| analyze_results | 只读指标生成解释（阶段一确定性） | 是（Analyst，阶段二接 LLM） |
| generate_report | Markdown 报告落盘 | 否 |
| generate_failure_report | 失败/拒绝/无候选时生成失败报告 | 否 |

> **与文档的 reconcile**：文档第 4 节流程图含 `generate_failure_report` 分支，但第 10 节骨架代码未列出该节点。本实现按第 4 节补齐该节点，使"无可评分候选""校验失败""用户拒绝"三类终止都有专门出口，更完整。

### 4.4 路由（`app/graph/routes.py`）

三个条件边路由函数：
- `route_after_validation`：有错误 → 失败报告；需审批 → 人工；否则 → 构建任务。
- `route_after_approval`：approved → 构建任务；否则 → 失败报告。
- `route_after_output_validation`：有成功且非空候选 → 评分；否则 → 失败报告。

### 4.5 模型适配器（`app/adapters/`）

- `base.py` 定义 `ModelAdapter` 协议（`runtime_checkable`）、`BaseAdapter` 基类、装饰器注册表 `@register_adapter`、`get_adapter()` 工厂、`registered_providers()`。
- `fake.py` 的 `FakeAdapter` 提供三个质量分明的模型：
  - **alpha**：高质量、正确完整。
  - **beta**：中等；JSON 用例输出畸形（验证规则评分）；安全用例模拟调用失败（验证失败分支）。
  - **gamma**：偏低；数值偏差、类型错误。
  - 行为依据 prompt 关键词判定，**确定性、可复现**。
- `provider_a.py` / `provider_b.py` 为阶段二脚手架，延迟导入 phase2 依赖，缺失时给出清晰错误，不影响阶段一运行。

### 4.6 服务层（`app/services/`）

- `executor.py`：`execute_batch` 用 `asyncio.gather` 并发；每个 `ModelConfig` 独立 `Semaphore` 限流；指数退避重试（3 次）；`request_key = run_id:case_id:provider:model`；**已完成任务从仓库加载、跳过调用，不重复扣费**。
- `scoring.py`：`rule_score` 确定性规则评分（JSON 合法性、安全拒绝、事实正确性、推导完整性）；`judge_candidate` 双次盲评（两次评分有确定性差异以演示方差）；`score_batch` **仅对成功候选评分**（对齐文档 9.4"对合格候选盲评"），综合分 = 0.4×规则 + 0.6×Judge。
- `metrics.py`：纯函数 `percentile`（线性插值）、`calculate_metrics` 输出每模型质量分/方差、P50/P95、token、成本、失败率，及异常标记（失败率>15%、评分方差>4、预算超限）。
- `reports.py`：Markdown 报告含运行配置、指标汇总表、异常、低分样本回放（质量分<6）、Analyst 分析、局限声明。

### 4.7 持久化（`app/repositories/runs.py`）

`RunRepository` 按 `runs/<run_id>/` 落盘：`model_results.json`、`score_results.json`（按 request_key 幂等 upsert）、`meta.json`、`report.md`。原始模型响应存文件，checkpoint 只存 ID 回链，避免 checkpoint 过大（对齐文档 8.1）。阶段三切换为 Supabase PostgreSQL + SQLAlchemy，接口不变。

### 4.8 Checkpointer（`app/graph/checkpointer.py`）

统一 `get_checkpointer(backend)` 工厂：
- `memory`：单元测试与单进程内联恢复。
- `sqlite`：**阶段一默认持久化**，支持 CLI `resume` 跨进程恢复。
- `postgres`：阶段三 LangGraph PostgreSQL Checkpointer。

> 关于"无 SQLite 迁移"原则（文档 6.1）：该原则针对**业务数据**（从第一天用 PostgreSQL）。此处 SQLite **仅用于 Agent 状态 checkpoint**，业务数据阶段一走 JSON 文件、阶段三走 PostgreSQL，不涉及 SQLite→PostgreSQL 业务数据迁移。

### 4.9 CLI 与 API

- `cli.py`（Typer）：`run`（支持 `--auto-approve` 与交互式审批）、`status`、`resume`（跨进程恢复）、`report`。
- `api/main.py`（FastAPI 骨架）：`/health`、`POST /runs`、`GET /runs/{id}/status`、`GET /runs/{id}/report`，阶段三加 Supabase Auth/RLS。

## 5. 状态图流转

```text
START -> intake_request -> plan_evaluation -> validate_plan
  -> [route_after_validation]
       errors?        -> generate_failure_report -> END
       approval?      -> human_approval (interrupt)
       else           -> build_tasks
human_approval -> [route_after_approval]
       approved       -> build_tasks
       rejected       -> generate_failure_report -> END
build_tasks -> execute_models -> validate_outputs
  -> [route_after_output_validation]
       has candidates -> score_results -> aggregate_metrics
                        -> analyze_results -> generate_report -> END
       no candidates  -> generate_failure_report -> END
```

## 6. 阶段一验证结果

### 6.1 质量门禁

```text
$ ruff check app tests        # All checks passed!
$ pytest                      # 36 passed in 3.89s
```

测试覆盖：路由函数（8）、指标纯函数（4）、规则评分与 Judge 方差（8）、执行器并发与幂等去重（3）、数据集白名单（5）、图集成含中断恢复与失败分支（8）。

### 6.2 端到端运行（Fake Adapter，5 案例 × 3 模型）

```text
$ agent-llm-stage run --config configs/run.example.yaml --auto-approve
status=completed, model_results=15, score_results=14, anomalies=1
```

| 模型 | 质量分 | 评分方差 | P50(ms) | P95(ms) | token | 成本 | 失败率 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| alpha | 9.50 | 0.09 | 120 | 120 | 65 | $0.0003 | 0% |
| beta  | 8.25 | 2.60 | 180 | 180 | 36 | $0.0001 | 20% |
| gamma | 8.52 | 0.50 | 260 | 260 | 49 | $0.0001 | 0% |

- `score_results=14`（非 15）：beta/case-004 调用失败，按文档 9.4 不参与评分，仅计入失败率。
- 异常标记：`high_failure_rate`（beta 失败率 20% > 15% 阈值）。
- 低分样本回放：case-003/beta（畸形 JSON，质量分 5.62，risk=hallucination）。

### 6.3 人工审批中断与跨进程恢复

```text
# 进程 A：跑到审批中断后退出（不恢复）
RUN_ID=model-comparison-v1-682d1e5c  paused=True

# 进程 B：新进程从 checkpoint 恢复
$ agent-llm-stage resume --run-id model-comparison-v1-682d1e5c --decision approved
status=completed, model_results=15   # 无重复调用
```

集成测试 `test_cross_connection_sqlite_resume` 用两个独立 sqlite 连接（模拟跨进程）验证：中断的 run 可恢复完成，且 `model_results=10` 无重复——**断点续跑不重复扣费**。

### 6.4 失败分支

- **用户拒绝**：`resume --decision rejected` → `generate_failure_report`，报告标注"用户拒绝执行评测"。
- **校验失败**：配置未注册 provider → `generate_failure_report`，报告列出错误。
- **无可评分候选**：单 case + beta（必失败）→ `generate_failure_report`。

### 6.5 幂等性

`test_execute_batch_idempotent_no_duplicate_calls`：同一批任务第二次执行时，已完成的任务从仓库加载、跳过调用，invoke 计数不增加——**失败重跑不产生重复扣费/写入**。

## 7. 关键设计决策

| 决策 | 原因 |
| --- | --- |
| State 列表用"全量替换"而非 add reducer | 失败重跑不重复累加，配合 request_key 去重实现幂等 |
| `model_dump(mode="json")` 写入 state | 枚举转纯字符串，checkpoint（msgpack）零警告、Postgres 友好 |
| 阶段一 LLM 节点为确定性实现 | 无需 API Key 即可跑通全流程并复现；结构兼容阶段二真实 LLM |
| `generate_failure_report` 独立节点 | 补齐文档第 4 节流程，三类终止有专门出口 |
| SQLite 仅用于 Agent checkpoint | 跨进程恢复；业务数据不碰 SQLite，符合文档 6.1 |
| 评分仅对成功候选 | 对齐文档 9.4"对合格候选盲评"，失败由失败率统计 |

## 8. 阶段二/三待办

**阶段二（真实模型与评分闭环）**
- `provider_a` / `provider_b` 接入真实 API，记录真实 token/成本。
- `judge_candidate` 替换为真实 LLM Judge（严格 JSON Schema 校验 + 有限重试）。
- `plan_evaluation` 支持自然语言请求（LLM 结构化输出 + `requires_clarification`）。
- 限流、超时、成本记录对接真实计费。

**阶段三（可信度与可展示性）**
- Supabase PostgreSQL 业务表 + RLS（`user_id = auth.uid()`），`RunRepository` 切换 SQLAlchemy 后端。
- LangGraph PostgreSQL Checkpointer（`get_checkpointer("postgres")` 已预留）。
- HTML 报告 + Plotly 图表（Jinja2）。
- FastAPI 完整化 + Supabase Auth。
- Docker Compose、GitHub Actions 回归集、演示视频。

## 9. 运行方式

```bash
# 激活虚拟环境（Windows Git Bash）
source .venv/Scripts/activate

# 模拟评测（无需 API Key）
agent-llm-stage run --config configs/run.example.yaml --auto-approve

# 交互式审批（会暂停等待输入 approved/rejected）
agent-llm-stage run --config configs/run.example.yaml

# 查看状态 / 跨进程恢复 / 查看报告
agent-llm-stage status --run-id <run_id>
agent-llm-stage resume --run-id <run_id> --decision approved
agent-llm-stage report --run-id <run_id>

# 测试与 lint
pytest
ruff check app tests
```

## 10. 结论

阶段一目标全部达成：Graph 骨架、模拟执行、节点路由、失败分支、checkpoint 恢复、幂等执行、CLI 与单元测试均已落地并通过验证。LLM 智能受边界约束（规划/评分/解释可由 LLM，调用/指标/落库/重试保持确定性），数据契约与可恢复执行能力完整，为阶段二接入真实模型与阶段三 Supabase/HTML 报告打好了可扩展的基础。

## 11. 阶段二：真实模型接入与 LLM 评分闭环

> 生成日期：2026-08-04。阶段二目标全部落地并通过 mock 验证；联网端到端实跑待 API Key。

### 11.1 概述

阶段二把阶段一预留的脚手架与 `NotImplementedError` 全部填成真实实现，fake 路径完整保留：

- 真实模型 adapter（`provider_a` SDK + `provider_b` httpx 裸调）接入任意 OpenAI 兼容端点，记录真实 token/成本。
- 真实 LLM Judge：`judge_candidate` 走 adapter + 严格 JSON Schema 校验 + 有限重试 + 失败降级。
- 真实 LLM Planner：自然语言请求 -> 结构化 `EvaluationPlan`，缺信息 `requires_clarification`。
- 真实计费：被测模型成本 + Judge 成本汇总。
- 多供应商标准模板（GLM / DeepSeek / 豆包 / Kimi）。

验证：`ruff check` 通过；`pytest 64 passed`（阶段一 36 + 阶段二 28，全 mock 不联网）；fake 端到端跑通无回归。

### 11.2 接口抽象（"留出接口接入真实 API"）

- 抽 `LLMEndpointConfig` 基类，`ModelConfig` / `JudgeConfig` / `PlannerConfig` 继承，连接配置统一：`base_url` / `api_key_env` / `temperature` / `max_tokens` / `timeout` / `pricing`。新字段全可选，旧 fake 配置零改动。
- `get_adapter(config)` 接受 `LLMEndpointConfig`，被测模型 / Judge / Planner 通用。
- `@register_adapter` 注册；新增 provider 只写一个 Adapter 类，核心流程（executor / graph）零改动。
- `provider_a`（langchain-openai SDK）与 `provider_b`（httpx 裸调）双实现，展示同一 `ModelAdapter` 协议的两种 OpenAI 兼容接入方式。
- `invoke(prompt, context, *, json_mode=False)` 增加 `json_mode`，Judge/Planner 结构化输出用；默认 False 向后兼容。

### 11.3 真实模型接入（`app/adapters/`）

- `provider_a.py`：`ChatOpenAI` 组装 messages（context 进 system），`json_mode` 用 `response_format={"type":"json_object"}`，从 `usage_metadata` 取 token，`compute_cost` 按 pricing（每 1M token USD）算成本，调用异常 `raise RuntimeError`（executor 重试）。
- `provider_b.py`：`httpx.AsyncClient` POST `{base_url}/chat/completions`，解析 `choices[0].message.content` 与 `usage`，HTTP 错误/异常包装为 RuntimeError。
- `base.py`：`resolve_api_key()` 优先 `api_key_env`，否则按 provider 推导（`provider_a`->`PROVIDER_A_API_KEY`），缺 key 抛清晰错误。
- `pricing.py`（新）：`compute_cost`；未配置 pricing 时 cost=0，token 照记（不臆造价格）。

### 11.4 真实 LLM Judge（`app/services/scoring.py`）

- `judge_candidate` 真实分支：`build_judge_prompt`（user）+ `prompts/judge_v1.txt`（system）-> `adapter.invoke(json_mode=True)` -> `_parse_judge_json` 严格校验（三维度 0-10、`risk_flag` 合法、必要字段）-> 有限重试（`JUDGE_PARSE_RETRIES=2`）-> 仍失败则降级为中性分 + `risk_flag=other`（不中断 run）。
- `JudgeResult` 新增 `prompt_tokens` / `completion_tokens` / `cost_usd`，Judge 成本可计入总成本。
- 容错：`strip_code_fence` 剥离模型可能包裹的 ` ```json ` 代码块。
- fake judge 路径（`_judge_heuristic`）保留不变。

### 11.5 真实 LLM Planner（`app/graph/nodes.py`）

- `plan_evaluation` 改 async；`request`（自然语言）存在时调 Planner LLM（`prompts/planner_v1.txt` + 可用 providers/datasets 列表）-> `_parse_plan_json` 解析 `EvaluationPlan`。
- 缺关键信息 -> `requires_clarification=True` + `missing_fields`；`validate_plan` 检测后转失败报告（列出 missing_fields）。
- Planner 降级：调用/解析失败 -> 返回需澄清的空计划。
- 放宽 `RunConfig`/`EvaluationPlan` 的 `models`/`dataset` 可空（Planner 产出前），非空校验移到 `validate_plan`；移除 `EvaluationPlan._models_nonempty` 自动校验。
- `RunConfig.planner: PlannerConfig | None`；`intake_request` 自然语言无 config 时提示"需 --config 提供 planner"。

### 11.6 计费与配置

- `metrics.py`：`calculate_metrics` 汇总被测模型成本 + Judge 成本 -> `total_cost_usd`，`budget_exceeded` 含全部真实成本。
- `cli.py`：启动 `load_dotenv()` 加载 `.env`（未装 python-dotenv 时跳过）；`python-dotenv` 从 phase3 提到 phase2。
- `configs/run.real.yaml`（新）：GLM / DeepSeek / 豆包 / Kimi 四家标准化模板，每个模型一个块，复制改 4 字段即增减供应商。
- `.env.example`：对应语义化 key 占位（`ZHIPU_API_KEY` / `DEEPSEEK_API_KEY` / `ARK_API_KEY` / `MOONSHOT_API_KEY` / `JUDGE_API_KEY` / `PLANNER_API_KEY`）。

### 11.7 验证结果

```text
$ ruff check app tests        # All checks passed!
$ pytest                      # 64 passed in 5.37s
```

新增测试（全 mock，不联网不花钱）：

| 文件 | 用例数 | 覆盖 |
| --- | --- | --- |
| `tests/test_schemas.py` | 7 | LLMEndpointConfig 继承、新字段默认值、旧配置兼容、空 models |
| `tests/test_provider_adapters.py` | 8 | provider_a/b token 解析、cost、json_mode 透传、异常包装、缺 key |
| `tests/test_planner.py` | 8 | 自然语言->EvaluationPlan、requires_clarification、降级、未配 planner |
| `tests/test_scoring.py`（扩展） | 5 | 真实 Judge 合法/代码块/非法降级/缺维度/调用失败降级 |

fake 端到端跑通（`run --config configs/run.example.yaml --auto-approve`）：`completed, model_results=15, score_results=14, anomalies=1`，与阶段一一致，代码改动无损。

### 11.8 阶段二文件改动

| 文件 | 改动 |
| --- | --- |
| `app/domain/schemas.py` | `LLMEndpointConfig` 基类；ModelConfig/JudgeConfig 继承；`PlannerConfig`；RunConfig+.planner；JudgeResult+token/cost；放宽 models/dataset |
| `app/adapters/base.py` | invoke+`json_mode`；`resolve_api_key`；get_adapter 接受 LLMEndpointConfig |
| `app/adapters/pricing.py`（新） | `compute_cost` |
| `app/adapters/provider_a.py` | 真实 langchain-openai |
| `app/adapters/provider_b.py` | 真实 httpx |
| `app/adapters/fake.py` | invoke+json_mode |
| `app/prompts.py`（新） | `load_prompt` 读 prompts/*.txt |
| `app/utils.py` | `strip_code_fence`（正则化）；`parse_json_lenient`（严格->json_repair） |
| `app/services/scoring.py` | 真实 Judge + 三层 JSON 容错（严格/json_repair/正则）+ 重试 + 降级 |
| `app/graph/nodes.py` | 真实 Planner；validate_plan 校验；Analyst 局限声明按 Judge provider 区分 |
| `app/services/metrics.py` | 汇总 Judge 成本 |
| `app/cli.py` | load_dotenv |
| `pyproject.toml` | python-dotenv、json-repair 提到 phase2 |
| `.env.example` / `configs/run.real.yaml`（新） | 多供应商模板（dotenv 安全注释格式） |
| `tests/` | 4 个测试文件新增/扩展（+34 用例） |

### 11.9 待办

- ~~**联网端到端实跑**~~：已完成，见 11.10。
- **阶段三**：Supabase PostgreSQL 业务表 + RLS、PostgreSQL Checkpointer、HTML 报告 + Plotly、FastAPI 完整化、Docker Compose。

### 11.10 联网端到端验证（已实跑）

使用真实 API Key 对 DeepSeek、Kimi（被测模型）与 glm-5.2 @ 火山方舟 coding 端点（Judge）完成端到端实跑：

```
agent-llm-stage run --config configs/run.real.yaml --auto-approve
# → completed, model_results=10, score_results=10, anomalies=0, total_cost_usd≈$0.015
```

| 被测模型 | provider | 质量分 | P95 延迟 | 成本 | 失败率 |
| --- | --- | --- | --- | --- | --- |
| deepseek-v4-flash | provider_b | 9.58 | ~4.0s | $0.0008 | 0% |
| kimi-k3 | provider_b | 9.23 | ~17.3s | $0.0035 | 0% |

Judge（glm-5.2）对每个候选双次盲评，产出真实 scores/evidence/raw_rationale（非降级）。

**实跑中发现并修复的问题：**

1. **Kimi `kimi-k3` 仅允许 `temperature=1`**：默认 0.0 触发 HTTP 400。在 `run.real.yaml` 的 Kimi 条目显式设置 `temperature: 1.0`。
2. **Judge 返回非合法 JSON 导致全量降级**：glm-5.2 会把候选回答（如 `{"name":"张三"}` 或含 `"米"` 引号的文本）原样塞进 `evidence`/`raw_rationale`，未转义双引号使 JSON 非法；火山方舟端点不强制 `json_mode`（`response_format` 被降级为 model_kwargs）。
   - 修复：`_parse_judge_json` 改为**三层容错**——① 严格 `json.loads` ② `json_repair` 修复（可挽救含未转义引号的 JSON，scores/risk 通常仍正确）③ 正则提取数值分数 + risk_flag（最鲁棒，因分数为数值、risk_flag 为已知枚举）。
   - 新增依赖 `json-repair>=0.30`（phase2）；`app/utils.py` 新增 `parse_json_lenient`（剥离代码块→严格→json_repair），Planner 解析复用之；`strip_code_fence` 改用正则，兼容完整/仅开头围栏与无语言标记。
   - `prompts/judge_v1.txt` 增补约束：只输出 JSON 对象、不裹代码块、字符串内转义引号。
3. **报告局限声明硬编码 "fake judge"**：`_deterministic_analysis` 改为按 `judge.provider` 区分——fake 走原表述，真实 Judge 改为 "Judge 为真实 LLM（provider），评分仍存在模型自身偏差"。
4. **`.env.example` 模板格式 bug**：`KEY=   # 注释` 会被 python-dotenv 解析为值 `# 注释`（非空），污染 `.env` 的 ZHIPU/ARK/PLANNER 行。改为**注释独占一行 + `KEY=` 空值**的 dotenv 安全格式，并清理 `.env` 中被污染的行（真实 key 未动）。

**多供应商标准化模板**（`configs/run.real.yaml` + `.env.example`）：每个供应商一个代码块（`provider`/`model`/`base_url`/`api_key_env`/`pricing`/可选 `temperature`），`.env.example` 每个供应商一个 key。增删模型只需复制/注释一个块 + 填/清对应 key。当前启用 DeepSeek + Kimi；智谱 GLM、豆包已注释为模板（填 key 后取消注释即可）。

**测试**：新增 6 用例覆盖 json_repair 挽救、正则降级、`parse_json_lenient`、`strip_code_fence` 变体；全量 70 用例通过，ruff 干净。
