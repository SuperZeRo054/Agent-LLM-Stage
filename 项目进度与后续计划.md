# 项目进度与后续计划

> 更新日期：2026-08-07
> 项目：Agent-LLM-Stage —— 基于 LangGraph 的 LLM 评测 Agent

---

## 一、总体路线图

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| **阶段一** | 框架搭建 + Fake Adapter 跑通全流程 | ✅ 完成（70 测试通过） |
| **阶段二** | 真实模型接入 + 真实 LLM Judge / Planner + 联网端到端验证 | ✅ 完成（已用真实 API 实跑通过） |
| **阶段三** | 可信度与可展示性：PostgreSQL 持久化、HTML 报告、FastAPI、Docker | ⬜ 待开始 |

当前进度：**阶段二已闭环，可立即用真实 API Key 对 DeepSeek / Kimi 等模型做端到端盲评并产出报告。下一步进入阶段三。**

---

## 二、已完成内容

### 2.1 阶段一：框架与确定性流程

- **LangGraph StateGraph**：11 节点（intake → plan → validate → execute → score → aggregate → analyze → report），职责单一、可测试。
- **可恢复执行**：每节点 checkpoint；中断后从最后成功节点恢复；幂等 `request_key` 去重，重跑不重复扣费。
- **Fake Adapter**：确定性模拟模型，无需 API Key 即可跑通全流程（`configs/run.example.yaml`）。
- **盲评 Judge（确定性 fake）**：候选顺序随机化、模型身份隐藏、双次评分取均值与方差。
- **CLI**：`run` / `status` / `resume` / `report`（Typer）。
- **运行产物落盘**：`runs/<run_id>/` 下 meta / model_results / score_results / report.md。

### 2.2 阶段二：真实模型接入与联网验证

**适配器抽象（留出真实 API 接口）**
- `LLMEndpointConfig` 基类（provider / model / base_url / api_key_env / temperature / max_tokens / timeout / pricing）→ `ModelConfig` / `JudgeConfig` / `PlannerConfig` 继承。
- `ModelAdapter` Protocol + `@register_adapter` 工厂 + `get_adapter(config)`。
- 两种真实实现证明接口通用性：
  - `provider_a`：langchain-openai SDK（含 `json_mode` 透传）。
  - `provider_b`：httpx 原生 HTTP（POST `/chat/completions` + Bearer 鉴权 + usage 解析）。
- `pricing.py`：按 1M token 计费，无定价返回 0（绝不臆造价格）。
- `resolve_api_key()`：从 `api_key_env` 指定的环境变量取 key，绝不硬编码、绝不返回前端。

**真实 LLM Judge**
- 盲评 prompt（`prompts/judge_v1.txt`）：3 维度（correctness/completeness/safety，0-10）+ risk_flag + evidence + raw_rationale。
- **三层 JSON 容错解析**（实跑中发现真实模型会把候选回答原样塞进 evidence，未转义引号致 JSON 非法；且部分端点不强制 json_mode）：
  1. 严格 `json.loads`
  2. `json_repair` 修复（挽救含未转义引号的 JSON，scores/risk 通常仍正确）
  3. 正则提取数值分数 + risk_flag（最鲁棒，分数为数值、risk_flag 为枚举）
- 有限重试 + 全失败降级中性分（不中断 run）。

**真实 LLM Planner**
- 自然语言请求 → 结构化 `EvaluationPlan`（`prompts/planner_v1.txt`）。
- `requires_clarification` 澄清回路；`validate_plan` 校验 models 非空。
- 复用 `parse_json_lenient` 容错解析。

**联网端到端实跑（已验证）**

```
agent-llm-stage run --config configs/run.real.yaml --auto-approve
# -> completed, model_results=10, score_results=10, anomalies=0, total_cost_usd≈$0.015
```

| 被测模型 | provider | 质量分 | P95 延迟 | 成本 | 失败率 |
| --- | --- | --- | --- | --- | --- |
| deepseek-v4-flash | provider_b | 9.58 | ~4.0s | $0.0008 | 0% |
| kimi-k3 | provider_b | 9.23 | ~17.3s | $0.0035 | 0% |

Judge（glm-5.2 @ 火山方舟 coding 端点）双次盲评产出真实 scores/evidence/rationale，**无降级**。

**多供应商标准化模板**（`configs/run.real.yaml` + `.env.example`）
- 每个供应商一个代码块：`provider` / `model` / `base_url` / `api_key_env` / `pricing` / 可选 `temperature`。
- 增删模型 = 复制/注释一个块 + 填/清 `.env` 对应 key。
- 当前启用 DeepSeek + Kimi；智谱 GLM、豆包已注释为模板（填 key 取消注释即纳入）。

**质量**
- 全量 **70 单元测试通过**，ruff 干净。
- 测试覆盖：schema、provider_a/b 适配器（mock）、Planner、scoring（含 json_repair 挽救 / 正则降级 / 容错解析 / 降级路径）、executor、graph、metrics、routes、utils。

---

## 三、当前可运行状态

### 3.1 环境与密钥

- 真实 key 仅存于 `.env`（已 gitignore）：DeepSeek、Moonshot(Kimi)、Judge 可用；ZHIPU/ARK/PLANNER 暂空。
- `.env.example` 为全空占位模板（dotenv 安全注释格式：注释独占一行 + `KEY=` 空值），可安全提交。
- **安全约束**：服务端密钥只放环境变量，绝不返回浏览器；不在响应中打印完整 key。

### 3.2 常用命令

```bash
# 安装（阶段一+二+开发工具）
pip install -e ".[phase2,dev]"

# 模拟评测（无 API Key）
agent-llm-stage run --config configs/run.example.yaml --auto-approve

# 真实评测（需 .env 填 key）
agent-llm-stage run --config configs/run.real.yaml --auto-approve

# 查看 / 恢复 / 报告
agent-llm-stage status  --run-id <run_id>
agent-llm-stage resume  --run-id <run_id> --decision approve
agent-llm-stage report  --run-id <run_id>
```

### 3.3 已知限制（不阻塞，待优化）

1. **数据集偏简单**：5 条用例（沸点/光速/方程/JSON/安全拒绝），强模型近乎满分，Judge 区分度不足 → 阶段三需扩充更有挑战的测试集。
2. **`rule_score` 不剥离代码块**：候选若用 ```json 包裹（如 kimi 的 JSON 用例），规则评分会判低分（Judge 仍正确）。
3. **真实 Planner 未联网验证**：`run.real.yaml` 暂未配 planner，自然语言请求路径仅 mock 覆盖。
4. **持久化为文件**：阶段三切换 PostgreSQL 前无法多实例/查询。

---

## 四、后续步骤：阶段三（可信度与可展示性）

> 目标：从"单机能跑"升级为"可信、可查询、可展示、可部署"。

### 步骤 1 ｜ PostgreSQL 持久化层

- **业务表设计**（SQLAlchemy 2.0 模型）：`runs` / `model_results` / `score_results` / `metrics` / `judge_scores`，与现有 Pydantic schema 对齐。
- **Repository 切换**：`RunRepository` 抽象 + 文件实现（现有）/ Postgres 实现；按 `CHECKPOINTER_BACKEND` 或独立 `DB_BACKEND` 选择。
- **Supabase RLS**：行级安全策略（用户只能查自己的 run），服务端 service_key 写入。
- **验收**：同一次 run 可从 Postgres 查回完整结果，与文件落盘一致。

### 步骤 2 ｜ LangGraph Checkpointer 切换

- 接入 `langgraph-checkpoint-postgres`，`CHECKPOINTER_BACKEND=postgres` 启用。
- 验证中断 → `resume` 从 Postgres checkpoint 恢复，幂等性不变。
- **验收**：进程重启后 `resume` 仍能从断点续跑。

### 步骤 3 ｜ HTML 可视化报告

- **Jinja2 模板**：在现有 Markdown 报告基础上生成 HTML。
- **Plotly 图表**：质量分对比柱状图、延迟 P50/P95 分布、成本/Token 对比、评分方差。
- 嵌入报告页，支持离线打开。
- **验收**：`report --run-id` 产出含交互图表的 HTML。

### 步骤 4 ｜ FastAPI 完整化

- 现有 `app/api/main.py` 骨架扩展为完整 REST API：
  - `POST /runs` 创建评测（提交配置/自然语言请求）
  - `GET /runs/{id}` 查状态
  - `GET /runs/{id}/report` 取报告（MD/HTML）
  - `GET /runs` 列表
- 异步执行 + 进度推送（SSE 或 WebSocket）。
- 鉴权：API Key / Supabase JWT；服务端密钥绝不外泄。
- **验收**：从 API 触发评测并轮询到完成，前端可展示进度。

### 步骤 5 ｜ Docker Compose 部署

- 服务：`app`（API+CLI）+ `postgres`（业务库 + checkpointer）+ 可选 Supabase 自托管。
- 一键 `docker compose up` 起本地完整环境。
- **验收**：容器化环境跑通一次真实评测。

### 步骤 6 ｜ 可信度增强（可选，视需求）

- **置信度**：基于样本量、评分方差、Judge 一致性给出结论置信区间。
- **异常检测**：失败率/延迟/成本突增自动标记（现 `anomalies` 字段已预留）。
- **Judge 校准**：人工标注集校准 Judge 偏差，去除"全 10 分"等宽放倾向。
- **更难测试集**：多轮对话、长上下文、代码生成、推理链等，提升区分度。

### 阶段三依赖（已在 `pyproject.toml` 声明）

```
sqlalchemy>=2.0, psycopg[binary]>=3.2, supabase>=2.7,
fastapi>=0.115, uvicorn>=0.30, jinja2>=3.1, plotly>=5.20,
langgraph-checkpoint-postgres>=2.0
```

---

## 五、建议的执行顺序

1. **步骤 1 + 2**（持久化）先行——是后续查询/部署的基础，且改动集中、可独立验收。
2. **步骤 3**（HTML 报告）——见效快、演示价值高，可与服务并行。
3. **步骤 4**（FastAPI）——依赖持久化层，提供对外接口。
4. **步骤 5**（Docker）——收尾，整合 1-4 成可部署产物。
5. **步骤 6**（可信度）——在真实使用中按痛点迭代。

---

## 六、相关文档

- `Agent-LLM-Stage-开发框架与实现方案.md` —— 原始设计文档（架构与契约）。
- `框架搭建报告.md` —— 阶段一/二详细实现记录（含第 11 节阶段二、11.10 联网验证）。
- `README.md` —— 快速开始与 CLI 说明。
- `configs/run.real.yaml` / `.env.example` —— 多供应商标准化模板。
